import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    headerContainer: {
        height: '40%',
        backgroundColor: '#F6727C',
        alignItems: 'center',
    },
    mainIcon: {
        marginTop: 40
    },
    mainHeader: {
        color: '#FFFFFF',
        marginTop:5,
        fontSize: 22
    },
    textHeader: {
        color: '#FFFFFF',
        fontSize: 18
    }
})

export default styles