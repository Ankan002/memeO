import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    greetingContainer: {
        marginTop: 45,
        width: '100%',
    },
    greetingText: {
        fontSize: 25,
        color: '#353E4C',
    }
})

export default styles